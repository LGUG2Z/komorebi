# toggle-workspace-float-override

```
Toggle a float override for new windows on the focused workspace, meaning that all new windows will open in floating mode for that workspace.

If no value was set for that workspace before, it will take the opposite of the current global value.

Usage: komorebic.exe toggle-workspace-float-override

Options:
  -h, --help
          Print help

```
